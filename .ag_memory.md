# [THE FOUNDATION]
- Core tech stack: React, Tailwind CSS, pdfjs-dist (v5.6.205)
- Routing: Component-based navigation (`PdfViewer`)
- Global state: React `useState` and `useRef` within `PdfViewer`. Annotation state in `annotationStore`.

# [THE FORGE]
- Feature: Refactoring `PdfViewer.tsx` for performance and stability. (COMPLETED)
- Files: `src/viewers/PdfViewer.tsx`, `src/index.css`
- Tasks:
  1. Prevent search toggle/query from re-initializing the entire PDF layout. [DONE] - Decoupled dependencies and used stateful refresh.
  2. Implement DOM virtualization for continuous mode. [DONE] - Using `visiblePages` state and checking intersection.
  3. Fix text layer selection alignment via CSS. [DONE] - Added `line-height: 1 !important` and related styles in `index.css`.
  4. Move search highlights to a separate overlay. [DONE] - Implemented `search-highlight-layer` with coordinate math.
  5. Prevent layout jumping on zoom by updating existing DOM nodes. [DONE] - Updated `render` to recycle wrappers.
  6. Decouple scroll observer from rendering state. [DONE] - Decoupled the logic and removed `isRenderingRef` gating.

# [THE GRAVEYARD]
- Direct DOM mutation of text nodes via `TreeWalker` was removed to prevent pdf.js coordinate breakage.
- `container.replaceChildren()` on every zoom was causing scroll jumps; replaced with in-place updates.
